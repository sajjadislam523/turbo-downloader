# Session 2026-07-11-001 — Code review & issue resolution

## What was done

Conducted a full codebase review, documented issues in `project-docs/ISSUES.md`, then fixed all actionable items:

### Bug fixes
- **Bug 1** — Removed unreachable `json.as_array()` dead code branch in `extract_source_info` (`src-tauri/src/lib.rs:130`)
- **Bug 2** — Replaced redundant yt-dlp validation in `start_keyword_download` with lightweight input-only checks, eliminating 2× yt-dlp probes on every keyword download start

### Code quality
- **CQ3** — Removed unused `postcss` from `devDependencies`
- **CQ4** — Removed unnecessary `async` from `vite.config.ts`

### Missing features
- **MF2** — Added `ErrorBoundary` class component (`src/ErrorBoundary.tsx`) wrapping `<App />` in `main.tsx`. Catches React render errors and shows a styled fallback with "Restart" button.
- **MF1** — Added download cancellation:
  - Rust: `DownloadState` (shared `Arc<Mutex<Option<u32>>>` PID store) managed by Tauri
  - Rust: `stop_download` command runs `kill <pid>` on the tracked process
  - Rust: `spawn_and_stream` / `stream_and_wait` now accept a pid_guard and clear it on exit
  - Frontend: Cancel button appears next to download button while `isDownloading` is true
  - Frontend: `cancelDownload` calls `invoke("stop_download")` and resets UI state

### Acknowledged future work
- Monolithic `App.tsx` (1339 lines) not refactored
- ESLint `exhaustive-deps` disable not addressed

## Build verification
- `npm run build` → TypeScript + Vite: OK
- `npm run tauri build` → Rust compilation + .deb bundling: OK
