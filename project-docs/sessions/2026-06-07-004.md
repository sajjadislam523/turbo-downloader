# Task: Correct Tauri project run command

Updated project instructions to use the README-backed Tauri development and build commands.

| filename | change type | what changed |
| --- | --- | --- |
| `AGENTS.md` | Changed | Replaced plain Vite and missing lint verification commands with Tauri commands from the README. |
| `.codex/plans/idea-inbox.md` | Changed | Captured the user's command correction as a durable project workflow idea. |
| `.codex/CHANGES.md` | Changed | Added a rolling changelog entry for the command correction. |
| `.codex/REVIEW.md` | Changed | Moved the missing lint script concern to resolved because the project rules were corrected. |
| `.codex/sessions/2026-06-07-004.md` | Added | Recorded this task, commands, outcomes, and concerns. |

## What was implemented and why

The README states that development should use `npm run tauri dev`, which starts Vite and opens the Tauri desktop window. It also states that production packaging should use `npm run tauri build`. The project instructions were corrected to use those commands instead of plain `npm run dev`, plain `npm run build`, and a nonexistent `npm run lint`.

This keeps Codex verification aligned with the actual desktop application workflow.

## Commands run

| command | result |
| --- | --- |
| `sed -n '1,260p' README.md` | pass: confirmed `npm run tauri dev` and `npm run tauri build` |
| `sed -n '1,220p' AGENTS.md` | pass: reviewed current project instructions |
| `sed -n '1,220p' .codex/plans/idea-inbox.md` | pass: reviewed existing idea inbox |
| `sed -n '1,220p' .codex/CHANGES.md` | pass: reviewed rolling changelog |
| `npm run tauri build` | pass: frontend built, Rust release build completed, and `.deb` bundle was created |
| `npm run tauri dev` with escalation | pass: Vite started and Cargo launched `target/debug/turbo-downloader` |
| `pgrep -af "tauri dev\|vite\|target/debug/turbo-downloader"` with escalation | pass: found Tauri dev, Vite, and app processes |
| `kill 497480 497509 497510 497592 497593 497642` with escalation | pass: stopped the Tauri dev verification processes |

## Concerns, flags, or TODOs

No open concerns for this command correction.
